{{>header}}
{{#if loggedIn}}
<div class="content">
    <section class="js">
        <h2>JavaScript</h2>
        <div class="articles">
            {{#if jsPosts}}
            {{#each jsPosts}}
            {{>article}}
            {{/each}}
            {{else}}
            <h3 class="no-articles">No articles yet</h3>
            {{/if}}
        </div>
    </section>
    <section class="CSharp">
        <h2>C#</h2>
        <div class="articles">
            {{#if cSharpPosts}}
            {{#each cSharpPosts}}
            {{>article}}
            {{/each}}
            {{else}}
            <h3 class="no-articles">No articles yet</h3>
            {{/if}}
        </div>
    </section>
    <section class="Java">
        <h2>Java</h2>
        <div class="articles">
            {{#if javaPosts}}
            {{#each javaPosts}}
            {{>article}}
            {{/each}}
            {{else}}
            <h3 class="no-articles">No articles yet</h3>
            {{/if}}
        </div>
    </section>
    <section class="Python">
        <h2>Python</h2>
        <div class="articles">
            {{#if pythonPosts}}
            {{#each pythonPosts}}
            {{>article}}
            {{/each}}
            {{else}}
            <h3 class="no-articles">No articles yet</h3>
            {{/if}}
        </div>
    </section>
</div>

{{!-- <!-- No articles message -->
<h3 class="no-articles">No articles yet</h3> --}}
{{else}}
{{/if}}
{{>footer}}